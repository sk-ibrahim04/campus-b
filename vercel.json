{
  "framework": "vite",
  "buildCommand": "npm run build --workspace=@campussynapse/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
